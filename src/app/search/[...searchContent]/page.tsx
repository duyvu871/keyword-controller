
interface SearchPageProps {
    children?: React.ReactNode
}

export default function SearchPage({}: SearchPageProps) {

    return (
        <div>
            <h1>Search Page</h1>
        </div>
    )
}